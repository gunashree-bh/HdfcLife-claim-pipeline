# HDFC Life Claim Pipeline
Open this folder in IntelliJ IDEA, select a JDK 17+ (JDK 21+ also works), open `src/com/hdfc/Main.java`, and Run `Main`.
The source tree follows the assignment's suggested `model`, `list`, `stack`, `queue`, `thread`, and `exception` packages.
